// Package main is the markup-svc HTTP server entry point. Today it
// is a scaffold; the HTTP layer lands in a subsequent commit after
// the first Decider adapter exists.
package main

import "fmt"

func main() {
	fmt.Println("markup-svc: scaffold only; see ADR-0001 for the domain port.")
}
